[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lmentor-windows"
version = "0.1.0"
description = "Compatibility scaffold for the Lmentor fusion project"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["lmentor", "lmentor.adapters"]

